export const CARD_CONSTANTS = {
    // Card size constraints
    MAX_CARD_WIDTH: 800,    // pixels
    MIN_CARD_WIDTH: 280,    // pixels
    CARD_ASPECT_RATIO: 0.66,  // height = width * aspect_ratio

    // Font size constraints
    MAX_FONT_SIZE: 62,      // pixels
    MIN_FONT_SIZE: 12,      // pixels

    // Scaling factors
    CONTENT_PADDING: 20,    // pixels of padding inside card
    TARGET_CHARS_PER_LINE: 15, // Ideal number of characters per line
};
